#include <iostream>

using namespace std;

int main()
{
    char ch;
    cin>>ch;
    if(ch == 'n' || ch == 'N')  cout<<"Naver D2\n";
    else                        cout<<"Naver Whale\n";
}