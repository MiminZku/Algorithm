#include <iostream>

using namespace std;

int n;

int main()
{
    cin.tie(nullptr); ios::sync_with_stdio(false);

    string str;
    while(getline(cin, str))
    {
        cout<<str<<endl;
    }
}